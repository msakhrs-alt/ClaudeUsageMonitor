# ClaudeUsageMonitor — デザイン改善指示

## 概要

メニューバー表示とポップオーバーUIのデザインを刷新する。
既存の機能・ロジックは一切変更しないこと。デザイン・見た目のみの変更。

---

## 1. メニューバー表示（StatusBarItem）

### 現状
テキストのみ: `41% | 4%`

### 変更後：ミニプログレスバー x2（縦並び）

NSStatusBarButtonのカスタムビューとして、以下を描画する：

```
┌─────────────────────┐
│ ████████░░░░  41%   │  ← セッション（上）
│ █░░░░░░░░░░░  4%    │  ← 週間（下）
└─────────────────────┘
```

**仕様：**
- バー幅: 52px、高さ: 4px、角丸: 2px
- バー間の垂直間隔: 4px
- バー右に数値テキスト（フォントサイズ: 10px、幅: 28px固定、右寄せ）
- 全体の高さ: メニューバーに収まるよう22px以内

**カラー：**
- バー背景（空部分）: `#111827`
- バー塗り（使用済み部分）:
  - 通常（< 80%）: `#3b82f6`（ブルー）
  - 警告（80〜95%）: `#f59e0b`（アンバー）
  - 危険（> 95%）: `#ef4444`（レッド）
- テキスト:
  - 通常: `#60a5fa`
  - 警告: `#fbbf24`
  - 危険: `#f87171`
- stale（データ古い）: バー・テキストともに `#374151`（グレーアウト）+ ⚠ をテキストに付加

**実装方法：**
`NSStatusBarButton` に `NSCustomView`（または `NSView` サブクラス）を設定し、`draw(_:)` で `NSBezierPath` を使って描画する。

---

## 2. ポップオーバー（ContentView）

### カラーテーマ：ディープスペース

SwiftUIの `ContentView` に以下のカラー定義を適用する。

**背景・ボーダー：**
- ポップオーバー背景: `#0a0f1e`
- セクションボーダー: `#1a2540`
- インナーバー背景: `#111827`

**テキスト：**
- セクションラベル（"SESSION" / "WEEKLY"）: `#4a6fa5`、フォントサイズ: 10px、letter-spacing あり
- パーセント数値: `#60a5fa`（通常）、`#fbbf24`（警告）、`#f87171`（危険）
- サブテキスト（リセット時刻など）: `#4a6fa5`

**プログレスバー：**
- バー背景: `#111827`
- バー塗り（通常）: `#3b82f6`
- バー塗り（警告 80〜95%）: `#f59e0b`
- バー塗り（危険 > 95%）: `#ef4444`
- 角丸: 3px、高さ: 6px

**ボタン（更新・終了）：**
- 背景: `#111827`
- テキスト: `#4a6fa5`
- ホバー: `#1a2540`
- 角丸: 6px

**全体レイアウト：**
- ポップオーバー幅: 260px（変更なし可）
- パディング: 16px
- セクション間スペース: 12px

---

## 3. 変更しないもの

- データ取得ロジック（ClaudeAPIService）
- 通知ロジック（NotificationService）
- 更新間隔設定（UserDefaults）
- ログイン処理（LoginWindowController）
- その他機能面のコード

---

## 実装順序

1. `AppDelegate.swift` のステータスバービュー描画部分を修正（ミニバーx2）
2. `ContentView.swift` のカラーをディープスペーステーマに更新
3. ビルドして動作確認
